services:
  bot:
    image: ${IMAGE}:pr-${PR_NUM}-latest
    container_name: tg-bot-pro-pr-${PR_NUM}
    restart: unless-stopped
    # Если нужна изоляция токенов на превью — раскомментируй и добавь секрет:
    # environment:
    #   - BOT_TOKEN=${PREVIEW_BOT_TOKEN}
    # env_file:
    #   - .env.preview
