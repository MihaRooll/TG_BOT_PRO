# compose.yml — прод
services:
  bot:
    image: ${IMAGE}:latest   # IMAGE устанавливаем в workflow (deploy job)
    container_name: tg-bot-pro
    restart: unless-stopped
    # Если нужно — добавь переменные/файлы окружения:
    environment:
      # - TZ=Europe/Moscow
      - TG_Token=${TG_Token}
    # env_file:
    #   - .env
